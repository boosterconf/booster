<%= form_for @talk, url: post_to do |f| %>

    <fieldset class="form-group">
    <legend>Language</legend>
    In what language will you deliver your talk? Please select the language you are most comfortable with.
    <p>
      <% if @talk.errors[:landuage].present? %>
          <div class="error-message">
            You must select a language.
          </div>
      <% end %>
    <label class="radio">
      <%= f.radio_button :language, 'English', required: '' %>
      English
    </label>
    <label class="radio">
      <%= f.radio_button :language, 'Norwegian' %>
      Norwegian
    </label>
    </p>
    </fieldset>
    <legend>Talk details</legend>
      The abstract will be published on the website as soon
      as you submit it, and will be used by the organizers during talk selection. It is therefore important that you
      provide a detailed abstract. This will help us to select in the selection process and increase the chances
      of your talk being selected.
    <fieldset class="form-group">
      <%= f.label :title, "Title" %>

      <%= f.text_field :title, :size => "80", :class => "form-control" %>
    </fieldset>
    <fieldset class="form-group">
      <%= f.label :description, "Abstract" %>
      <%= f.text_area :description, :class => "tinymce span10" %>
      <% if @talk.errors[:description].present? %>
        <div class="error-message">
          You must provide an abstract.
        </div>
      <% end %>
    </fieldset>

    <%= tinymce %>

    <fieldset class="form-group">
      <%= f.label :equipment, "Leave comments for the organizers here." %>
      <%= f.text_area :equipment, :class => 'form-control', :rows => 5  %>
    </fieldset>

    <% if admin? %>
    <fieldset class="form-group">
      <%= f.label :video_url, "Vimeo URL" %>
      <%= f.text_field :video_url, :size => "80", :class => "form-control" %>
    </fieldset>
    <% end %>

      Please be aware that by submitting a talk to Booster, you agree to follow our
      <%= link_to 'Code of Conduct', info_coc_path, target: '_new' %>.
    <div class="form-actions">
      <%= f.submit @talk.new_record? ? 'Next' : 'Save changes', :class => 'btn btn-primary' %>
    </div>
<% end %>
