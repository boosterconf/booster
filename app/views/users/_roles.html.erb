<%# TODO: When this partial is used to edit existing user, need
    to check whether user has selected role. Now we always display
    all of them unselected. %>


<h2>What role(s) do you have?</h2>
<fieldset class="form-group">

  <% Roles.all.each do |role| %>
      <div class="checkbox">
      <label>
        <%= check_box_tag "roles[]", role, @user.has_role?(role.to_s), id: "roles_#{role}" %>
        <%= Roles.label[role] %>
      </label>
      </div>
  <% end %>
</fieldset>
