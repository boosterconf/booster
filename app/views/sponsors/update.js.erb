$("#notice").html("<%= escape_javascript(render :partial => 'shared/notice' , :locals => { :flash => flash }).html_safe %>");

$('#row-id-<%=@sponsor.id%>').html("<%= escape_javascript(render :partial => 'sponsors/tablerow', :locals => { :sponsor => @sponsor } ) %>");

$('#stats').html("<%= escape_javascript(render :partial => 'sponsors/stats' ) %>");

$('#events').html("<%= escape_javascript(render @events ) %>");

$.notify("<%= flash[:notice] %>", "success");

<%
#Avoid flash notice from ajax calls showing up on the next request
flash[:notice] = nil
%>
