<p id="notice"><%= notice %></p>

<h2>Invoice</h2>

<legend>Recipient:</legend><p><%= @invoice.delivery_method == 'email' ? @invoice.email : @invoice.adress %></p>
<legend>Total (ex. MVA)</legend><p><%= @invoice.total %></p>

<legend>Tickets in invoice (<%= @invoice.registrations.size %>)</legend>

<table class="table table-striped">
  <tr>
  	<th>Name</th>
  	<th>Email</th>
  	<th>Price</th>
  </tr>
  <% @invoice.registrations.each do |registration| %>
  	<tr>
  		<td><%= registration.user.name %></td>
  		<td><%= registration.user.email %></td>
  		<td><%= registration.price %></td>
  	</tr>
  <% end %>
</table>
