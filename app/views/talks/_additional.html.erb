<legend>Additional speakers</legend>
<% if @workshop.speakers.size > 1 %>
    <p>This workshop is presented by: </p>
    <ul>
      <% @workshop.speakers.each do |speaker| %>
        <li><%= speaker.user.name %></li>
      <% end %>
    </ul>
<% else %>
    <p>If you want to facilitate the workshop together with somebody else, please enter their email address here. We'll
      send him or her
      an email so that they can finish their registration.</p>
    <div class="field email">
      <%= f.label :additional_speaker_email %>
      <%= text_field_tag(:additional_speaker_email, "", :class => 'input-xlarge') %>
    </div>
<% end %>