<% content_for :title, "Suggested topics" %>
<h2 class="page_title">Suggested talks
  <span class="feed" title="Watch new talks">
    <%= link_to talks_path(:format => :rss) do %>
    <%= image_tag('/images/icon_feed.png') %>
    <% end %>
  </span>
</h2>

<p>
  <a href="#workshops" class=   "btn btn-primary btn-large">View suggested workshops</a>
</p>

<p>Every abstract is visible to everyone, in order to encourage discussion. Comments from both organizers and
  others are also visible.</p>


<% if !@lightning_talks.empty? %>
    <h4 id="lightning">Lightning talks</h4>
    <%= render :partial => @lightning_talks %>
<% end %>

<% if !@workshops.empty? %>
    <h4 id="workshops">Workshops</h4>
    <%= render :partial => @workshops %>
<% end %>

<p>Missing anything?
<br/>  <%= link_to 'Suggest your own talk!', info_cfp_url, :class => "btn btn-primary btn-large"  %>
</p>
