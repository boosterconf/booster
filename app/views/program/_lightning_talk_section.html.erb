<% cache_unless admin?, talks do %>
  <section class='period lightning-talk'>
    <header>
      <span class='location'><%= location %></span>
    </header>
    <div class='slot lightning-talk-slot'>
      <% talks.each do |talk| %>
        <a class="single-talk" href="<%= talk_path(talk.id) %>">
          <div class='talk' data-talkid='<%= talk.id %>'>
            <h4><%= talk.title %>
              <% if talk.video_url.present? %>
                <%= fa_icon("film") %>
              <% end %>
              <% if talk.has_slides? %>
                <i class="fa fa-sticky-note" title="Talk has link to slides"></i>
              <% end %>              
            </h4>
            <span class='author'><%= talk.speaker_name %></span>
            <%= render partial: 'adminInformation', locals: {talk: talk} %>
          </div>
        </a>
      <% end %>
    </div>
  </section>
<% end %>